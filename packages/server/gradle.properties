# Ensure the Gradle daemon/workers use UTF-8 for file and path encoding.
# Important when the project path contains non-ASCII characters.
org.gradle.jvmargs=-Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8
