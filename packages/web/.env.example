# ai-test-forge web 환경변수 템플릿
# 실제 값을 채워 .env 로 복사한 뒤 사용 (.env 는 git 에 커밋하지 않음)
# Vite 는 VITE_ 접두사 변수만 클라이언트에 노출한다.
# 주의: FE 는 브라우저에 노출되므로 시크릿(키/토큰)을 절대 넣지 않는다.

# ai-test-forge 서버 API base URL
VITE_API_BASE_URL=http://localhost:8080
