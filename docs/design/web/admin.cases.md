---
status: done
last-updated: 2026-09-14
ref: docs/specs/pages/admin.md
---

# 관리자 페이지 케이스

## 참조 기획

- [관리자 페이지](../../specs/pages/admin.md)

## 레이아웃

별도 페이지. 전체 화면, 테이블 뷰 기반.
관리자 전용 (일반 사용자에게 미노출 — 라우트 가드가 비-admin을 `"/"`로 리다이렉트).

## 작업 범위

- **A(완료)**: 스펙 관리 목록(Case 1) + 스펙 상세(Case 2). 스펙 상태 관리(비활성/활성/삭제) + RBAC(관리자 전용 화면).
- **B(현재 작업)**: 사용자 관리 목록(Case 3) + 계정 생성 모달(Case 4) + 역할/상태 변경 ConfirmModal + 비밀번호 변경 모달. RBAC 뼈대 위에서 사용자 CRUD·역할/상태·비밀번호 지정을 구현한다.

## 케이스 목록

| # | 케이스 | 범위 | 설명 |
|---|--------|------|------|
| 1 | 스펙 관리 목록 | **A** | 등록 서비스 + 상태(ACTIVE/INACTIVE) + API 수 + 액션(비활성/활성/삭제) |
| 1e | 스펙 관리 빈 상태 | **A** | 등록된 스펙이 하나도 없을 때 |
| 2 | 스펙 상세 | **A** | 기본 정보 + 서비스 설명(읽기 전용) + API 목록 + 인증 프로필 |
| 3 | 사용자 관리 목록 | **B** | 사용자 목록 + 역할/상태 배지 + 검색/카운트 + 액션(승격/강등/비번/비활성/활성) |
| 3e | 검색 결과 없음 | **B** | 아이디 검색 결과가 하나도 없을 때 |
| 4 | 계정 생성 모달 | **B** | 아이디 + 이름(선택) + 비밀번호 + 역할(라디오) 폼 모달 |
| 5 | 역할 변경 ConfirmModal | **B** | 승격/강등 확인 (다음 요청부터 반영 안내) |
| 6 | 비밀번호 변경 모달 | **B** | 관리자가 새 비밀번호 직접 지정 (8자+, 임시발급 아님) |
| 7 | 상태 변경 ConfirmModal | **B** | 비활성화 확인(로그인 차단 + 세션 만료 안내). 활성화는 즉시 |

---

## 스펙 관리 목록 (Case 1) — A

- 관리자 전용 화면(`/admin/specs`). 비-admin은 라우트 가드가 `"/"`로 리다이렉트하므로 화면 자체가 관리자에게만 노출된다.
- 테이블: **서비스명 | baseUrl | 상태 | API 수 | 액션**.
- 상태 배지는 **색상 + 텍스트**로 표기(색상만으로 구분 금지): 🟢 ACTIVE / ⚪ INACTIVE.
- **INACTIVE 행은 흐리게(dimmed)** 표시(예: URL 변경으로 남은 좀비 스펙).
- 액션 버튼:
  - ⏸️ 비활성화 (ACTIVE일 때) → **ConfirmModal**
  - ▶️ 활성화 (INACTIVE일 때) → 즉시(확인 불필요, 복귀 성격)
  - 🗑️ 삭제 (소프트 삭제) → **ConfirmModal**
- 행 클릭 → 상세 페이지(`/admin/specs/:id`).
- 관리자 목록은 **전체(INACTIVE 포함)** 조회(`includeInactive=true`, ADMIN만). 삭제(소프트)된 스펙은 목록에서도 제외.
- 참조: [스펙 등록 - 재등록 정합성](../../specs/spec/registration.md)

### 빈 상태 (Case 1e)

- 등록된 스펙이 하나도 없으면 `empty-state`로 "등록된 스펙이 없습니다" 안내.
- 외부 서버가 client-spring 라이브러리로 등록하면 자동 노출됨을 함께 안내.

---

## 스펙 상세 (Case 2) — A

엔드포인트가 많아 **모달이 아닌 별도 페이지**(`/admin/specs/:id`).

- 헤더: **← 목록으로** + 서비스명 + [비활성화]/[활성화] + [삭제].
- 기본 정보: baseUrl / 상태(색상+텍스트 배지) / 등록 시각.
- **서비스 설명(읽기 전용)**: description / domain / capabilities / notes 표시.
  - 관리자 수정본이 있으면 그것을, 없으면 yml 원본을 표시(관리자 우선 규칙).
  - **편집은 별도 작업** — 이번 A에서는 읽기 전용. 편집 버튼 없음(또는 비활성).
  - yml 값 변경 감지 시 "변경 감지" 경고 + 변경 내용 보기(강제 덮어쓰기 없음).
- **API 엔드포인트 목록**: method + path + summary. 스펙에서 사라진 API는 **DEPRECATED 뱃지**(참조 레시피는 실행 전 경고).
- **인증 프로필**: name + loginPageUrl.
- 참조: [스펙 등록 방식](../../specs/spec/registration.md)

---

## 스펙 상태 / 수동 관리 (A)

| 액션 | 동작 | 확인 |
|------|------|------|
| 비활성화 | STATUS = INACTIVE. AI 매칭/실행 대상에서 제외 | **ConfirmModal** |
| 활성화 | INACTIVE → ACTIVE 복귀 (관리자만) | 즉시 |
| 삭제 | 소프트 삭제. 참조 레시피는 유효성 검증에서 경고 | **ConfirmModal** |

- 삭제/비활성은 되돌리기 부담이 있어 ConfirmModal로 한 번 확인. 활성화는 복귀 성격이라 즉시.

---

## 사용자 관리 목록 (Case 3) — B

관리자 전용 화면(`/admin/users`). 비-admin은 라우트 가드가 `"/"`로 리다이렉트.

- 상단 툴바: [🔍 아이디 검색] + 결과 카운트("총 N명") + [+ 사용자 추가].
- 테이블: **아이디 | 이름 | 역할 | 상태 | 마지막 접속 | 액션**.
  - 역할 배지: 관리자(`badge--info`) / 사용자(`badge--neutral`).
  - 상태 배지: **색상 + 텍스트**(색상만으로 구분 금지) — 활성(`badge--success`) / 비활성(`badge--error`).
  - INACTIVE 행은 흐리게(dimmed) 표시.
- 액션 버튼(아이콘 + `aria-label`/`title`):
  - 🔼 관리자로 승격 (USER일 때) → **역할 변경 ConfirmModal**
  - 🔽 사용자로 강등 (ADMIN일 때) → **역할 변경 ConfirmModal**
  - 🔑 비밀번호 변경 → **비밀번호 변경 모달**
  - ⏸️ 비활성화 (ACTIVE일 때) → **상태 변경 ConfirmModal**
  - ▶️ 활성화 (INACTIVE일 때) → 즉시(확인 불필요, 복귀 성격)
- **계정 삭제 액션은 없다** — 계정 정리는 비활성화로 대체한다(삭제 버튼 미노출).
- **본인 행**: 아이디 옆 "(나)" 배지. 본인의 강등/비활성 버튼은 `aria-disabled="true"` + 툴팁("본인 계정은 변경할 수 없습니다"). 비밀번호 변경만 가능.
- **마지막 ACTIVE ADMIN**: ACTIVE ADMIN이 1명뿐이면 그 계정의 강등/비활성 버튼은 `aria-disabled="true"` + 툴팁("마지막 관리자는 변경할 수 없습니다"). ADMIN이 2명 이상이면 (본인이 아닌) 다른 ADMIN은 강등/비활성이 가능하다. 본인이 유일한 ACTIVE ADMIN이면 두 사유("본인 계정" + "마지막 관리자")를 병기한다.
- 위험 액션 게이팅은 UX일 뿐이고 실제 차단은 서버가 400으로 강제한다.

### 데모 목록 데이터 구성 (게이팅 3종 구분)

디자인 HTML의 Case 3 목록은 마지막 ACTIVE ADMIN 보호를 눈으로 구분할 수 있도록 ADMIN을 2명 이상으로 구성한다.

| 아이디 | 역할 | 상태 | 게이팅 |
|--------|------|------|--------|
| admin (나) | 관리자 | 활성 | 본인 사유로 강등/비활성 `aria-disabled`. ADMIN 2명이라 '마지막' 사유는 아님 |
| admin2 | 관리자 | 활성 | 게이팅 없음 — ADMIN이 2명이라 마지막이 아니므로 강등/비활성 가능 |
| user1 | 사용자 | 활성 | 승격/비번/비활성 모두 가능 |
| user2 | 사용자 | 비활성 | 승격/비번/활성(즉시). 행은 dimmed |

- admin2의 🔽 강등은 실제로 **역할 변경 ConfirmModal(강등)**을 띄운다: "'admin2'를 일반 사용자로 강등할까요? 관리자 권한을 잃습니다."
- 참조: [사용자 관리 (B)](../../specs/pages/admin.md#사용자-관리-b-adminusers)

### 검색 결과 없음 (Case 3e)

- 아이디 검색 결과가 하나도 없으면 `empty-state`로 "검색 결과가 없습니다" 안내.
- 로그인한 관리자 본인이 최소 1명 존재하므로 목록 전체가 비는 경우는 없다(검색 필터 결과만 빔).

---

## 계정 생성 모달 (Case 4) — B

[+ 사용자 추가] 클릭 시 **폼 모달**로 노출. 셀프 회원가입 없음 — 관리자가 직접 생성.

- 입력 필드(모두 `label for`/`id` 연결):
  - **아이디**(text): 3~50자, 공백 불가. `form-hint`로 규칙 안내.
  - **이름**(text, 선택): 100자 이하.
  - **비밀번호**(password): 8자 이상. `form-hint`로 안내.
  - **역할**(라디오): 일반 사용자 / 관리자.
- 액션: [취소] / [생성].
- 모달은 `role="dialog"` `aria-modal="true"` + `aria-labelledby`.
- 참조: [계정 생성 정책](../../specs/common/auth.md#계정-생성-정책)

---

## 역할 변경 ConfirmModal (Case 5) — B

- 승격: "**'user1'**을 관리자로 승격할까요?" / 강등: "**'admin2'**를 일반 사용자로 강등할까요? 관리자 권한을 잃습니다."
- 안내: "변경은 다음 요청부터 반영됩니다."
- 값 입력 없이 확인만. `role="dialog"` `aria-modal="true"`.

---

## 비밀번호 변경 모달 (Case 6) — B

- 제목: "비밀번호 변경 — {아이디}".
- 새 비밀번호(password, 8자 이상) 입력. `label for`/`id` 연결 + `form-hint`.
- 관리자가 새 비밀번호를 **직접 지정**(임시 비밀번호 자동 발급 아님).
- 액션: [취소] / [변경].

---

## 상태 변경 ConfirmModal (Case 7) — B

- 비활성화: "**'user1'** 계정을 비활성화할까요? 로그인이 차단되며, 진행 중인 세션은 다음 요청에서 만료됩니다."
- 활성화(INACTIVE→ACTIVE)는 복귀 성격이라 **즉시** 처리(확인 불필요).
- `role="dialog"` `aria-modal="true"`.

---

## 사용자 관리 액션 요약 (B)

| 액션 | UI | 확인 |
|------|-----|------|
| 사용자 추가 | **폼 모달** | 생성 |
| 비밀번호 변경 | **폼 모달** | 변경 |
| 역할 변경 (승격/강등) | **ConfirmModal** | 확인만 |
| 비활성화 | **ConfirmModal** | 확인만 |
| 활성화 | 즉시 | 없음 |

---

## RBAC (A)

- 관리자 전용 화면(`/admin/**`). 라우트 가드(RequireAdmin)가 비-admin을 `"/"`로 리다이렉트.
- nav 노출은 UX 게이팅일 뿐이며, 실제 권한은 서버가 매 요청 재확인해 강제한다.
- 화면 상단에 "관리자 전용" 표시(admin 배지)로 맥락을 명확히 한다.

## 접근성

- 테이블은 `thead`/`tbody` 구조, 각 열에 `th scope="col"`.
- 상태 배지에 **텍스트 포함**(색상만으로 구분하지 않음).
- 삭제/비활성 액션은 **ConfirmModal** 확인. 모달은 `role="dialog"` `aria-modal="true"`.
- 액션 아이콘 버튼에 `aria-label`/`title` 제공.
- 행 상세 이동은 클릭뿐 아니라 **키보드 경로**를 제공한다. 디자인 HTML에서는 서비스명을 링크형 버튼(`cell-name__link`)으로 노출해 Tab/Enter로 상세 진입이 가능하다. 액션 셀은 `stopPropagation`으로 행 클릭과 분리한다.
- **사용자 관리(B)**: 폼 모달(계정 생성/비밀번호 변경)은 `label for`/`id`로 입력을 연결하고 `role="dialog"` `aria-modal="true"` + `aria-labelledby`를 둔다. 위험 액션(본인/마지막 ACTIVE ADMIN의 강등·비활성) 버튼은 `disabled` 대신 `aria-disabled="true"` + 이유 툴팁으로 노출해 스크린리더가 상태를 읽을 수 있게 한다.
- 사용자 관리 탭(B, 미구현)은 tab 패턴에 맞게 `disabled` 대신 `aria-disabled="true"`로 표기한다.

### FE 구현 필수 (디자인 HTML 한계 — 명세로만 표기)

디자인 HTML로는 재현하기 어려워 명세로만 남긴다. FE 구현 시 반드시 처리한다.

- **ConfirmModal 포커스 관리**: 열릴 때 모달 내부(취소/확인 버튼)로 초기 포커스 이동, 닫힐 때 트리거 버튼으로 포커스 복귀, `ESC`로 닫기, 열린 동안 **포커스 트랩**(모달 밖으로 Tab 이동 금지).
- **행 상세 이동 키보드 경로**: 서비스명 링크 버튼 외에, 행 자체에도 키보드 접근이 필요하면 `tabindex`/`role` + Enter 핸들러로 보강한다(디자인 HTML은 서비스명 버튼까지만 제공).
- **`aria-disabled` 버튼 no-op**: 위험 액션 게이팅으로 `aria-disabled="true"`가 걸린 버튼(본인/마지막 ACTIVE ADMIN의 강등·비활성)은 클릭·Enter 시 **동작을 억제(no-op)**하되 포커스는 유지한다. `disabled` 대신 `aria-disabled`를 쓰는 이유는 스크린리더가 버튼과 사유 툴팁을 읽을 수 있게 하기 위함이므로, 포커스 가능 상태는 그대로 두고 핸들러에서만 조기 반환한다.
