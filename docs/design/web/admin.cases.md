---
status: done
last-updated: 2026-09-14
ref: docs/specs/pages/admin.md
---

# 관리자 페이지 케이스

## 참조 기획

- [관리자 페이지](../../specs/pages/admin.md)

## 레이아웃

별도 페이지. 전체 화면, 테이블 뷰 기반.
관리자 전용 (일반 사용자에게 미노출 — 라우트 가드가 비-admin을 `"/"`로 리다이렉트).

## 작업 범위

- **A(현재 작업)**: 스펙 관리 목록(Case 1) + 스펙 상세(Case 2). 스펙 상태 관리(비활성/활성/삭제) + RBAC(관리자 전용 화면).
- **B(다음 작업)**: 사용자 관리(Case 3) + 계정 생성(Case 4). 케이스는 유지하되 이번 스코프 아님.

## 케이스 목록

| # | 케이스 | 범위 | 설명 |
|---|--------|------|------|
| 1 | 스펙 관리 목록 | **A** | 등록 서비스 + 상태(ACTIVE/INACTIVE) + API 수 + 액션(비활성/활성/삭제) |
| 1e | 스펙 관리 빈 상태 | **A** | 등록된 스펙이 하나도 없을 때 |
| 2 | 스펙 상세 | **A** | 기본 정보 + 서비스 설명(읽기 전용) + API 목록 + 인증 프로필 |
| 3 | 사용자 관리 | B (다음 작업) | 사용자 목록 + 역할/상태 + [+ 사용자 추가] |
| 4 | 계정 생성 | B (다음 작업) | 아이디 + 비밀번호 + 역할 직접 입력 폼 |

---

## 스펙 관리 목록 (Case 1) — A

- 관리자 전용 화면(`/admin/specs`). 비-admin은 라우트 가드가 `"/"`로 리다이렉트하므로 화면 자체가 관리자에게만 노출된다.
- 테이블: **서비스명 | baseUrl | 상태 | API 수 | 액션**.
- 상태 배지는 **색상 + 텍스트**로 표기(색상만으로 구분 금지): 🟢 ACTIVE / ⚪ INACTIVE.
- **INACTIVE 행은 흐리게(dimmed)** 표시(예: URL 변경으로 남은 좀비 스펙).
- 액션 버튼:
  - ⏸️ 비활성화 (ACTIVE일 때) → **ConfirmModal**
  - ▶️ 활성화 (INACTIVE일 때) → 즉시(확인 불필요, 복귀 성격)
  - 🗑️ 삭제 (소프트 삭제) → **ConfirmModal**
- 행 클릭 → 상세 페이지(`/admin/specs/:id`).
- 관리자 목록은 **전체(INACTIVE 포함)** 조회(`includeInactive=true`, ADMIN만). 삭제(소프트)된 스펙은 목록에서도 제외.
- 참조: [스펙 등록 - 재등록 정합성](../../specs/spec/registration.md)

### 빈 상태 (Case 1e)

- 등록된 스펙이 하나도 없으면 `empty-state`로 "등록된 스펙이 없습니다" 안내.
- 외부 서버가 client-spring 라이브러리로 등록하면 자동 노출됨을 함께 안내.

---

## 스펙 상세 (Case 2) — A

엔드포인트가 많아 **모달이 아닌 별도 페이지**(`/admin/specs/:id`).

- 헤더: **← 목록으로** + 서비스명 + [비활성화]/[활성화] + [삭제].
- 기본 정보: baseUrl / 상태(색상+텍스트 배지) / 등록 시각.
- **서비스 설명(읽기 전용)**: description / domain / capabilities / notes 표시.
  - 관리자 수정본이 있으면 그것을, 없으면 yml 원본을 표시(관리자 우선 규칙).
  - **편집은 별도 작업** — 이번 A에서는 읽기 전용. 편집 버튼 없음(또는 비활성).
  - yml 값 변경 감지 시 "변경 감지" 경고 + 변경 내용 보기(강제 덮어쓰기 없음).
- **API 엔드포인트 목록**: method + path + summary. 스펙에서 사라진 API는 **DEPRECATED 뱃지**(참조 레시피는 실행 전 경고).
- **인증 프로필**: name + loginPageUrl.
- 참조: [스펙 등록 방식](../../specs/spec/registration.md)

---

## 스펙 상태 / 수동 관리 (A)

| 액션 | 동작 | 확인 |
|------|------|------|
| 비활성화 | STATUS = INACTIVE. AI 매칭/실행 대상에서 제외 | **ConfirmModal** |
| 활성화 | INACTIVE → ACTIVE 복귀 (관리자만) | 즉시 |
| 삭제 | 소프트 삭제. 참조 레시피는 유효성 검증에서 경고 | **ConfirmModal** |

- 삭제/비활성은 되돌리기 부담이 있어 ConfirmModal로 한 번 확인. 활성화는 복귀 성격이라 즉시.

---

## 사용자 관리 (Case 3) — B (다음 작업)

> **B(사용자 관리)에서 구현.** 이번 A 범위 아님. 케이스 HTML은 위치 확보용으로 유지한다.

- 사용자 목록 + 역할/상태 + [+ 사용자 추가].

## 계정 생성 (Case 4) — B (다음 작업)

> **B(사용자 관리)에서 구현.** 이번 A 범위 아님. 케이스 HTML은 위치 확보용으로 유지한다.

- 셀프 회원가입 없음 — 관리자가 이 화면에서 계정을 직접 생성.
- 입력 필드: 아이디(text) / 비밀번호(password) / 역할(select: 일반 사용자·관리자).
- 참조: [로그인, 권한/역할](../../specs/common/auth.md)

---

## RBAC (A)

- 관리자 전용 화면(`/admin/**`). 라우트 가드(RequireAdmin)가 비-admin을 `"/"`로 리다이렉트.
- nav 노출은 UX 게이팅일 뿐이며, 실제 권한은 서버가 매 요청 재확인해 강제한다.
- 화면 상단에 "관리자 전용" 표시(admin 배지)로 맥락을 명확히 한다.

## 접근성

- 테이블은 `thead`/`tbody` 구조, 각 열에 `th scope="col"`.
- 상태 배지에 **텍스트 포함**(색상만으로 구분하지 않음).
- 삭제/비활성 액션은 **ConfirmModal** 확인. 모달은 `role="dialog"` `aria-modal="true"`.
- 액션 아이콘 버튼에 `aria-label`/`title` 제공.
- 행 상세 이동은 클릭뿐 아니라 **키보드 경로**를 제공한다. 디자인 HTML에서는 서비스명을 링크형 버튼(`cell-name__link`)으로 노출해 Tab/Enter로 상세 진입이 가능하다. 액션 셀은 `stopPropagation`으로 행 클릭과 분리한다.
- 사용자 관리 탭(B, 미구현)은 tab 패턴에 맞게 `disabled` 대신 `aria-disabled="true"`로 표기한다.

### FE 구현 필수 (디자인 HTML 한계 — 명세로만 표기)

디자인 HTML로는 재현하기 어려워 명세로만 남긴다. FE 구현 시 반드시 처리한다.

- **ConfirmModal 포커스 관리**: 열릴 때 모달 내부(취소/확인 버튼)로 초기 포커스 이동, 닫힐 때 트리거 버튼으로 포커스 복귀, `ESC`로 닫기, 열린 동안 **포커스 트랩**(모달 밖으로 Tab 이동 금지).
- **행 상세 이동 키보드 경로**: 서비스명 링크 버튼 외에, 행 자체에도 키보드 접근이 필요하면 `tabindex`/`role` + Enter 핸들러로 보강한다(디자인 HTML은 서비스명 버튼까지만 제공).
